import type { Metadata } from "next";
import Link from "next/link";
import { internationalDestinations } from "@/data/international";
import { buildPageMetadata } from "@/lib/seo";

export const metadata: Metadata = buildPageMetadata({ title: "Destinations | Festival Talent 2027", description: "Découvrez les territoires de l'édition 2027 : Sénégal, Paris et Rome.", path: "/destinations" });

export default function DestinationsPage() { return <main className="min-h-screen bg-black px-6 pb-24 pt-36 text-white sm:px-10 lg:px-20"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.3em] text-yellow-300">Territoires internationaux</p><h1 className="mt-5 text-5xl font-black uppercase sm:text-7xl">Sénégal · Paris · Rome</h1><p className="mt-6 max-w-2xl text-base leading-8 text-white/65">Chaque destination accueille une étape différente du parcours des talents. Les lieux et dates seront précisés au fur et à mesure des confirmations.</p><div className="mt-12 grid gap-5 lg:grid-cols-3">{internationalDestinations.map((destination) => <article key={destination.slug} className="rounded-[2rem] border border-yellow-300/20 bg-white/[.05] p-8"><h2 className="text-3xl font-black uppercase">{destination.name}</h2><p className="mt-2 text-sm font-black uppercase tracking-[.14em] text-yellow-300">{destination.subtitle}</p><p className="mt-5 text-sm leading-7 text-white/60">{destination.description}</p><p className="mt-6 text-xs font-black uppercase tracking-[.14em] text-yellow-200">{destination.status}</p><Link href={`/destinations/${destination.slug}`} className="mt-8 inline-flex text-xs font-black uppercase tracking-[.18em] text-white underline decoration-yellow-300 underline-offset-8">Explorer</Link></article>)}</div></div></main>; }
