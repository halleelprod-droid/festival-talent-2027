import type { Metadata } from "next";
import Link from "next/link";
import { internationalDestinations } from "@/data/international";
import { buildPageMetadata } from "@/lib/seo";

export const metadata: Metadata = buildPageMetadata({ title: "Destinations | Festival Talent 2027", description: "Chaque destination joue un rôle spécifique dans le parcours Festival Talent 2027 : le Sénégal détecte et révèle, Paris connecte et rend visible, Rome crée et dialogue.", path: "/destinations" });

export default function DestinationsPage() { return <main className="min-h-screen bg-black px-6 pb-24 pt-36 text-white sm:px-10 lg:px-20"><div className="mx-auto max-w-7xl"><p className="text-xs font-black uppercase tracking-[.3em] text-yellow-300">Territoires internationaux</p><h1 className="mt-5 text-5xl font-black uppercase sm:text-7xl">Une édition, trois territoires</h1><p className="mt-6 max-w-3xl text-base leading-8 text-white/65">Chaque destination joue un rôle spécifique dans le parcours Festival Talent 2027. Le Sénégal constitue le point d’ancrage et de détection. Paris ouvre des connexions professionnelles et diasporiques. Rome développe le dialogue culturel et créatif.</p><div className="mt-12 grid gap-5 lg:grid-cols-3">{internationalDestinations.map((destination) => <article key={destination.slug} className="rounded-[2rem] border border-yellow-300/20 bg-white/[.05] p-8"><div className="flex items-center justify-between"><span className="text-xs font-black text-yellow-300">{destination.code}</span><span className="text-[10px] font-black uppercase tracking-[.14em] text-yellow-200">{destination.statusLabel}</span></div><h2 className="mt-7 text-3xl font-black uppercase">{destination.name}</h2><p className="mt-2 text-sm font-black uppercase tracking-[.14em] text-yellow-300">{destination.signature}</p><p className="mt-5 text-sm leading-7 text-white/60">{destination.role}</p><div className="mt-8 flex flex-wrap gap-4"><Link href={`/destinations/${destination.slug}`} className="inline-flex text-xs font-black uppercase tracking-[.18em] text-white underline decoration-yellow-300 underline-offset-8">Explorer l’étape</Link><Link href={`/international-interest?destination=${destination.slug}`} className="inline-flex text-xs font-black uppercase tracking-[.18em] text-yellow-200 underline underline-offset-8">Manifester son intérêt</Link></div></article>)}</div><p className="mt-10 text-sm leading-7 text-white/45">La programmation évolue selon les confirmations officielles. Les informations encore en préparation sont signalées sur chaque page destination.</p></div></main>; }
